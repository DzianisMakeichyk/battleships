import styled from 'styled-components';

export const AreaContainerStyled = styled.div`
	position: relative;
	height: 100vh;
`;

export const AreaWrapperStyled = styled.div`
	position: absolute;
	top: 150px;
	left: 50%;
	transform: translateX(-50%);
`;

export const ResteButtonWrapperStyled = styled.div`
	display: flex;
	margin: 20px auto 0;
	justify-content: center;
`;
